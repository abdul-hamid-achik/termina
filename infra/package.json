{
  "name": "termina-infra",
  "private": true,
  "description": "Pulumi (TypeScript) IaC for the Termina DigitalOcean deployment. Isolated from the app — its own deps, tsconfig, and node_modules.",
  "main": "index.ts",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@pulumi/digitalocean": "^4.40.0",
    "@pulumi/pulumi": "^3.140.0"
  }
}
