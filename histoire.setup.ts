// Histoire global setup. Loads the terminal theme (Tailwind 4 entry + :root CSS
// vars + JetBrains Mono fonts) so every story iframe is styled exactly like the
// app. Pinia + Nuxt-auto-import shims for store/composable-coupled components are
// added here in a later step.
import './app/assets/css/terminal.css'
